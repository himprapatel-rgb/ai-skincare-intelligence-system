# Railway config for frontend (when root = frontend)
# Uses Dockerfile to avoid Nixpacks npm cache EBUSY error
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
